//To be used with mocha --require babel-polyfill --compilers js:babel-register
//Import * as aModule from '../src/aModule';
import * as chai from 'chai';

let should = chai.should(),
    expect = chai.expect;

describe ("EL Interface tests", function() {

    describe("", function() {
        
        it("", function() {
            should.exist(5);
        });

    });
    
});
